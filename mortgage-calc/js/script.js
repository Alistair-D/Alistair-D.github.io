function calculate() {
    const P = number(document.getElementById('principal').value);
    const r = number(document.getElementById('rate').Value)/100;
    const n = number(document.getElementById('payments').value);
    const t = number(document.getElementById('years').value);
    const m = number(document.getElementById('payment').value);
}
